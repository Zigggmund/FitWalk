import React from 'react';
import { Text, View } from 'react-native';

const BorderedInput = () => {
  return (
    <View>
      <Text>BorderedInput</Text>
    </View>
  );
};

export default BorderedInput;
