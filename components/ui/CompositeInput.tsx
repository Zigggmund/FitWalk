import React from 'react';
import { Text, View } from 'react-native';

interface BorderedInputProps {
  disable?: boolean;
}

const BorderedBlockInput = () => {
  return (
    <View>
      <Text>BorderedInput</Text>
    </View>
  );
};

export default BorderedBlockInput;
